@model Core.ViewModels.SignInViewModel

<div class="row justify-content-center">

    <div class="col-lg-6 col-md-6 col-sm-12">

        <h2>Sign in</h2>

        <form asp-action="" asp-controller="Accounts" method="post">

            <div class="text-danger" asp-validation-summary="All"></div>

            <div class="form-group">
                <label asp-for="@Model.Email" class="control-label"></label>
                <input asp-for="@Model.Email" class="form-control input-form" />
                <span class="text-danger" asp-validation-for="@Model.Email" />
            </div>

            <div class="form-group">
                <label asp-for="@Model.Password" class="control-label"></label>
                <input asp-for="@Model.Password" class="form-control input-form" />
                <span class="text-danger" asp-validation-for="@Model.Password" />
            </div>

            <div class="form-group">
                <input type="submit" value="Sign in" class="btn btn-outline-primary" />
                <a asp-action="Register" asp-controller="Accounts" class="btn btn-outline-info">Register</a>
            </div>

        </form>

    </div>

</div>
